{
  "name": "insightify-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "tailwindcss -i styles/index.css -o style.css --minify && esbuild index.tsx --bundle --outfile=index.js --jsx=automatic --external:react --external:react/* --external:react-dom/* --external:@google/genai --external:recharts --external:gsap --external:gsap/* --external:styled-components && npm install --prefix api"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "esbuild": "^0.23.0",
    "postcss": "^8.4.39",
    "tailwindcss": "^3.4.6"
  }
}