{
  "name": "insightify-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "mkdir -p dist && tailwindcss -i styles/index.css -o dist/style.css --minify && esbuild index.tsx --bundle --outfile=dist/bundle.js --jsx=automatic --external:react --external:react/* --external:react-dom/* --external:@google/genai --external:recharts --external:gsap --external:gsap/* --external:styled-components && npm install --prefix api"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "esbuild": "^0.23.0",
    "postcss": "^8.4.39",
    "tailwindcss": "^3.4.6"
  }
}